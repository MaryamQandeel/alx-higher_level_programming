#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    for i in matrix:
        for j in range(len(i)):
            new_num = j*j
           new_matrix = print("{:d}".format(new_num), end="")
             
       
